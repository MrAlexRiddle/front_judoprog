import React from 'react';
import classes from "./ButtonForm.module.css";

const ButtonForm = (props) => {
    return (
        <button className={classes.BtnForm} {...props}>

        </button>
    );
};

export default ButtonForm;